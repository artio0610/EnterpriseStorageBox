// Create the plugin index.
APT::Update::Post-Invoke-Success { "which esb-mkaptidx >/dev/null 2>&1 || exit 0; esb-mkaptidx || true"; };
DPkg::Post-Invoke { "which esb-mkaptidx >/dev/null 2>&1 || exit 0; esb-mkaptidx || true"; };
